import { Meta, Canvas } from "@storybook/blocks";
import { moduleMetadata } from '@storybook/angular';
import * as chartStories from './barChart.stories'


<Meta title="Visualisation Types/Diagrams/Bar Chart/Bar Chart"/>

# Bar Chart
## Table of contents
- [Description](#description)
- [Anatomy](#anatomy)
- [Chart variations](#chart-variations)
- [Do's and don'ts](#dos-and-donts)

## Description
A Bar Chart is similar to a Column Chart, both show comparisons among categories. The only difference is in the horizontal or vertical layout, by a simple inversion of the axes. The Bar Chart will have horizontal bars whereas the Column Chart will have vertical columns. The horizontal layout is the best choice for long text labels and trends, whereas the vertical layout is the best choice for short labels and trends.

In a Bar Chart, the Y-axis represents the categories being compared while the X-axis represents a measured value. The Y-axis will generally be set with a categorical ordinal scale. The length of the bars is proportional to the values displayed.
<Canvas of={chartStories.Description} />
## Anatomy
<img src="../images/barChart_anatomy.png" alt="alt"/>
## Chart variations
### Bar chart with a confidence interval
<Canvas of={chartStories.Confidence} />
The lower and upper margin of the confidence interval is shown by an overlay. Usually we use the 95%-margin which has to be mentioned in the legend. In charts with only one colour, the hatched area is two steps darker than the bar chart. If there are multiple colours or different shades of the base colour, we use black #333333 instead.
### Bar chart with an overlay (mark?)
<Canvas of={chartStories.Overlay} />
### Bar chart in groups
<Canvas of={chartStories.Groups} />
### Bar chart with diverging bars
<Canvas of={chartStories.divergent} />
## Do's and don'ts
### Axis starts at 0
Diagrams must always start at 0 otherwise the perception is distorted (unless the diagram represents an index, in which case the 100 value is mandatory). Deviations are allowed if the interruption is clearly marked. 
### Horizontal layout
All charts with long labels, have to be rotated by 90° to be compatible with small screens (mobile). 
### Sort unordered categories by metric
Appropriate sorting, which is almost always based on a metric, is important to effectively convey information in a bar chart and increase readability.
